{
  "private": true,
  "scripts": {
    "start": "sanity dev",
    "dev": "sanity dev",
    "build": "sanity build",
    "cors:add": "npx sanity cors add",
    "deploy": "sanity deploy",
    "prestart": "npm run env",
    "predeploy": "npm run env",
    "env": "copy ..\\.env .env.development || copy ..\\.env.local .env.development",
    "studio:deploy": "npx dotenv -- npm --prefix studio run deploy"
  },
  "dependencies": {
    "@sanity/vision": "^3.5.1",
    "prop-types": "^15.7",
    "react": "^18",
    "react-dom": "^18",
    "sanity": "^3.5.1",
    "sanity-plugin-asset-source-unsplash": "^1.0.6",
    "styled-components": "^5.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "postcss": "^8.4.21",
    "tailwindcss": "^3.2.4"
  }
}
